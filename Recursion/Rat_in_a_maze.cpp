#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<vector<int>> Maze;
    for(int i=0; )
    for (auto x : Maze)
    {
        for (auto y : x)
        {
            cout << y << " ";
        }
    }
}